/**
 * Design-system re-export so skeletons are discoverable alongside the other
 * `ui/` primitives. Implementation lives in `components/system`.
 */
export { LoadingSkeleton as Skeleton, LoadingSkeletonText as SkeletonText } from '@/components/system/LoadingSkeleton'
