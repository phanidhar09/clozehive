# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.29

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
aiosqlite==0.22.1
greenlet==3.5.0
alembic==1.18.4
pgvector==0.4.2

# ── Cache ─────────────────────────────────────────────────────────────────────
# Pinned <6 because arq (below) requires redis>=4.2,<6. Our usage is basic
# (get/set/setex/delete/exists/ping/scan_iter) and the ai-worker already runs
# this combination, so 5.x is safe. Bump only if arq adds redis>=6 support.
redis[hiredis]>=5.0.1,<6

# ── Async task queue (enqueue jobs to the ai-worker over Redis) ───────────────
arq>=0.26.0

# ── Auth / Security ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
passlib==1.7.4

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1
tenacity==9.1.4
openai==2.37.0
langsmith==0.8.5

# ── Validation ────────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==25.5.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9
limits==5.8.0

# ── Firestore ────────────────────────────────────────────────────────────────
firebase-admin==7.4.0

# ── Misc ─────────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
orjson==3.11.9
pillow==12.2.0

# ── Gemini AI (new SDK — avoids google-ai-generativelanguage protobuf<6 pin) ──
google-genai==2.6.0

# ── Google Cloud Storage (persistent image storage) ───────────────────────────
google-cloud-storage==3.10.1

# ── Pinned Google transitive deps ─────────────────────────────────────────────
# firebase-admin, google-generativeai, and google-cloud-storage all share this
# cluster of transitive packages. Pinning them explicitly prevents pip/uv from
# having to resolve the complex inter-package constraints at install time.
google-api-core==2.30.3
google-api-python-client==2.196.0
google-auth==2.53.0
google-auth-httplib2==0.4.0
googleapis-common-protos==1.75.0
google-cloud-core==2.6.0
google-cloud-firestore==2.27.0
google-resumable-media==2.9.0
# grpcio, grpcio-status, google-crc32c: NOT pinned — platform-specific natives,
# and grpcio-status version depends on Python version (>=1.75.1 on Py >=3.14).
# protobuf: NOT pinned — grpcio-status>=1.75.1 requires >=6.31.1; pinning 5.x
# would conflict. Let the resolver pick a compatible 6.x version.
proto-plus==1.28.0

# ── RAG / Vector search ───────────────────────────────────────────────────────
# faiss-cpu: in-memory FAISS index for local dev / testing (no GPU required).
# In production pgvector (already listed above) is used instead.
faiss-cpu==1.13.2
