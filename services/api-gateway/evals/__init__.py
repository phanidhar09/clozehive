"""ClozeHive offline eval harness.

A golden-set evaluation of the *deterministic* parts of the AI pipeline —
model routing and output grounding/validation — so that prompt and threshold
changes can be measured instead of guessed. See ``evals/README.md``.
"""
